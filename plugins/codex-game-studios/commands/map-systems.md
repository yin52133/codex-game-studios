# /map-systems

Decompose a game concept into individual systems, map dependencies, prioritize design order, and create the systems index. (source alias: /map-systems).

## Arguments

- `input`: [next | system-name] [--review full|lean|solo]

## Workflow

1. Use the `$codex-game-studios:map-systems` skill.
2. Read only the role, template, rule, or engine references that the skill requests.
3. Apply repository `AGENTS.md` instructions before editing files.
4. Report the concrete artifact paths and verification result.

## Examples

```text
/map-systems <task details>
```
