# /perf-profile

Structured performance profiling workflow. Identifies bottlenecks, measures against budgets, and generates optimization recommendations with priority rankings. (source alias: /perf-profile).

## Arguments

- `input`: [system-name or 'full']

## Workflow

1. Use the `$codex-game-studios:perf-profile` skill.
2. Read only the role, template, rule, or engine references that the skill requests.
3. Apply repository `AGENTS.md` instructions before editing files.
4. Report the concrete artifact paths and verification result.

## Examples

```text
/perf-profile <task details>
```
