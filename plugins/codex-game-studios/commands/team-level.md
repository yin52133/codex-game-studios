# /team-level

Orchestrate level design team: level-designer + narrative-director + world-builder + art-director + systems-designer + qa-tester for complete area/level creation. (source alias: /team-level).

## Arguments

- `input`: [level name or area to design] [--review full|lean|solo]

## Workflow

1. Use the `$codex-game-studios:team-level` skill.
2. Read only the role, template, rule, or engine references that the skill requests.
3. Apply repository `AGENTS.md` instructions before editing files.
4. Report the concrete artifact paths and verification result.

## Examples

```text
/team-level <task details>
```
