# /start

First-time onboarding — asks where you are, then guides you to the right workflow. No assumptions. (source alias: /start).

## Arguments

- `input`: [no arguments]

## Workflow

1. Use the `$codex-game-studios:start` skill.
2. Read only the role, template, rule, or engine references that the skill requests.
3. Apply repository `AGENTS.md` instructions before editing files.
4. Report the concrete artifact paths and verification result.

## Examples

```text
/start <task details>
```
