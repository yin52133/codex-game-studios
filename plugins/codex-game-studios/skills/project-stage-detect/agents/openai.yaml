interface:
  display_name: "Project Stage Detect"
  short_description: "Automatically analyze project state, detect stage, identify gaps, and recommend next steps based on existing artifacts. "
  default_prompt: "Use $codex-game-studios:project-stage-detect for this game-development workflow."
